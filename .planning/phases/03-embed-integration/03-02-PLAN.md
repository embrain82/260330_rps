---
phase: 03-embed-integration
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/screens/ResultScreen.tsx
  - src/components/screens/__tests__/ResultScreen.test.tsx
  - next.config.ts
  - public/test-embed.html
autonomous: false
requirements: [INTG-01, INTG-03]

must_haves:
  truths:
    - "Victory screen shows coupon code when couponConfig is present"
    - "Victory screen shows coupon image when couponImage is provided"
    - "Victory screen shows congratulations only when no couponConfig"
    - "Game loads correctly inside an iframe with no X-Frame-Options blocking"
    - "test-embed.html demonstrates full bidirectional postMessage flow"
  artifacts:
    - path: "src/components/screens/ResultScreen.tsx"
      provides: "Conditional coupon display UI"
      contains: "couponConfig"
    - path: "src/components/screens/__tests__/ResultScreen.test.tsx"
      provides: "Tests for coupon display and fallback"
    - path: "next.config.ts"
      provides: "iframe embedding headers (CSP frame-ancestors, X-Frame-Options)"
      contains: "frame-ancestors"
    - path: "public/test-embed.html"
      provides: "Embed test harness with postMessage logging"
      contains: "RPS_COUPON_CONFIG"
  key_links:
    - from: "src/components/screens/ResultScreen.tsx"
      to: "src/store/gameStore.ts"
      via: "useGameStore((s) => s.couponConfig)"
      pattern: "couponConfig"
    - from: "public/test-embed.html"
      to: "src/components/Game.tsx"
      via: "iframe src=/ embedding"
      pattern: "iframe.*src"
    - from: "next.config.ts"
      to: "HTTP response headers"
      via: "headers() config"
      pattern: "frame-ancestors"
---

<objective>
Build the coupon victory UI, configure iframe embedding headers, and create the embed test harness.

Purpose: Complete the user-facing integration surface -- the coupon screen players see on victory, the HTTP headers that allow iframe embedding, and the test page that validates the full postMessage contract end-to-end.

Output: ResultScreen with conditional coupon display, next.config.ts with iframe headers, test-embed.html for QA.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-embed-integration/03-CONTEXT.md
@.planning/phases/03-embed-integration/03-RESEARCH.md
@.planning/phases/03-embed-integration/03-01-SUMMARY.md

<interfaces>
<!-- Types and store from Plan 01 that this plan depends on -->

From src/lib/rps/types.ts (after Plan 01):
```typescript
export interface CouponConfig {
  couponCode: string
  couponImage?: string
  couponText?: string
}

export interface RpsGameWinMessage {
  type: 'RPS_GAME_WIN'
  payload: SessionPayload
}
```

From src/store/gameStore.ts (after Plan 01):
```typescript
interface GameStore {
  // ... existing fields ...
  couponConfig: CouponConfig | null
  setCouponConfig: (config: CouponConfig) => void
}
```

From src/components/screens/ResultScreen.tsx (current):
```typescript
export function ResultScreen() {
  const phase = useGameStore((s) => s.phase)
  const retry = useGameStore((s) => s.retry)
  // ... renders victory/gameover with retry button
}
```

From src/components/Game.tsx (after Plan 01):
```typescript
// Root div has className="relative min-h-screen overflow-hidden [contain:content]"
// usePostMessage() is called in component body
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade ResultScreen with conditional coupon display and add tests</name>
  <files>src/components/screens/ResultScreen.tsx, src/components/screens/__tests__/ResultScreen.test.tsx</files>
  <read_first>
    - src/components/screens/ResultScreen.tsx
    - src/store/gameStore.ts (Plan 01 version with couponConfig)
    - src/lib/rps/types.ts (Plan 01 version with CouponConfig)
    - .planning/phases/03-embed-integration/03-CONTEXT.md (D-03, D-04)
    - .planning/phases/03-embed-integration/03-RESEARCH.md (Code Example 4)
  </read_first>
  <behavior>
    - Test: When phase==='victory' and couponConfig has couponCode, the coupon code text is displayed
    - Test: When phase==='victory' and couponConfig has couponImage, an img element renders with that src
    - Test: When phase==='victory' and couponConfig has couponText, the text is displayed
    - Test: When phase==='victory' and couponConfig is null, fallback text "5판 연속 승리!" is displayed (no coupon area)
    - Test: When phase==='gameover', neither coupon nor fallback victory text is shown
    - Test: Retry button is always visible in both victory and gameover
  </behavior>
  <action>
    1. **Create test file src/components/screens/__tests__/ResultScreen.test.tsx** first:

    ```typescript
    import { describe, it, expect, beforeEach } from 'vitest'
    import { render, screen } from '@testing-library/react'
    import { useGameStore } from '@/store/gameStore'
    import { ResultScreen } from '@/components/screens/ResultScreen'
    ```

    Test cases:
    - Set `useGameStore.setState({ phase: 'victory', couponConfig: { couponCode: 'SAVE-50' } })`, render, assert `screen.getByText('SAVE-50')` exists
    - Set `useGameStore.setState({ phase: 'victory', couponConfig: { couponCode: 'X', couponImage: 'https://example.com/coupon.png' } })`, render, assert `screen.getByRole('img')` has src containing 'coupon.png'
    - Set `useGameStore.setState({ phase: 'victory', couponConfig: { couponCode: 'X', couponText: '10% 할인' } })`, render, assert `screen.getByText('10% 할인')` exists
    - Set `useGameStore.setState({ phase: 'victory', couponConfig: null })`, render, assert `screen.getByText('5판 연속 승리!')` exists, assert `screen.queryByTestId('coupon-area')` is null
    - Set `useGameStore.setState({ phase: 'gameover', couponConfig: null })`, render, assert retry button exists with text '다시 하기'
    - Both phases: assert retry button exists (`screen.getByRole('button')`)

    2. **Modify src/components/screens/ResultScreen.tsx**:

    - Add import: `import type { CouponConfig } from '@/lib/rps'` (for type reference only)
    - Add selector: `const couponConfig = useGameStore((s) => s.couponConfig)`
    - Replace the static `<p>` text for victory with conditional coupon rendering per D-03/D-04:
      - If `phase === 'victory' && couponConfig`:
        - Render a `data-testid="coupon-area"` container div
        - If `couponConfig.couponImage`: render `<img src={couponConfig.couponImage} alt="쿠폰" className="w-full max-w-[280px] rounded-xl mb-4 mx-auto" />`
        - Always render coupon code: `<p className="text-2xl font-mono font-bold text-white bg-white/10 rounded-xl px-6 py-3 tracking-wider select-all">{couponConfig.couponCode}</p>`
        - If `couponConfig.couponText`: render `<p className="text-base text-white/80 mt-2">{couponConfig.couponText}</p>`
      - If `phase === 'victory' && !couponConfig`:
        - Render `<p className="text-base text-white mb-12">5판 연속 승리!</p>` (graceful fallback per D-03)
      - If `phase === 'gameover'`:
        - Keep existing `<p>다시 도전해보세요!</p>`
    - Keep the existing heading "축하합니다!" / "아쉽네요..." logic
    - Keep the existing retry button unchanged
    - Coupon code should have `select-all` class so users can easily copy

    Write tests FIRST, run (should fail), then implement UI changes, then run tests (should pass).
  </action>
  <verify>
    <automated>cd /Users/nonsleeper/Documents/Coding/260330 && npx vitest run src/components/screens/__tests__/ResultScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - test -f src/components/screens/__tests__/ResultScreen.test.tsx
    - grep -q "couponConfig" src/components/screens/ResultScreen.tsx
    - grep -q "coupon-area" src/components/screens/ResultScreen.tsx
    - grep -q "couponImage" src/components/screens/ResultScreen.tsx
    - grep -q "couponCode" src/components/screens/ResultScreen.tsx
    - grep -q "couponText" src/components/screens/ResultScreen.tsx
    - grep -q "5판 연속 승리" src/components/screens/ResultScreen.tsx
    - npx vitest run src/components/screens/__tests__/ResultScreen.test.tsx exits 0
  </acceptance_criteria>
  <done>
    ResultScreen shows coupon code/image/text when couponConfig is present.
    ResultScreen shows "5판 연속 승리!" fallback when no couponConfig.
    Gameover screen unchanged (shows "다시 도전해보세요!").
    All 6+ ResultScreen tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure next.config.ts iframe headers and create test-embed.html harness</name>
  <files>next.config.ts, public/test-embed.html</files>
  <read_first>
    - next.config.ts
    - .planning/phases/03-embed-integration/03-CONTEXT.md (D-09, D-11)
    - .planning/phases/03-embed-integration/03-RESEARCH.md (Pattern 4, Pattern 6, Code Example 1)
  </read_first>
  <action>
    1. **Modify next.config.ts** to add iframe embedding headers per D-09:

    ```typescript
    import type { NextConfig } from 'next'

    const allowedOrigins = process.env.NEXT_PUBLIC_ALLOWED_ORIGIN || '*'

    const nextConfig: NextConfig = {
      async headers() {
        return [
          {
            source: '/:path*',
            headers: [
              {
                key: 'Content-Security-Policy',
                value: `frame-ancestors ${allowedOrigins}`,
              },
              {
                key: 'X-Frame-Options',
                value: 'ALLOWALL',
              },
            ],
          },
        ]
      },
    }

    export default nextConfig
    ```

    2. **Create public/test-embed.html** per D-11:

    A self-contained HTML page that:
    - Embeds the game in an iframe at `/` with 375x667px (mobile viewport)
    - Has a "Send Coupon Config" button that sends `RPS_COUPON_CONFIG` to the iframe
    - Has input fields for couponCode (default "TEST-COUPON-2026"), couponText (default "10% 할인 쿠폰"), and couponImage (optional URL)
    - Has a real-time log panel (`<pre>`) that displays received `RPS_GAME_WIN` messages as pretty-printed JSON
    - Includes a timestamp on each log entry
    - Has basic styling (centered layout, readable fonts, visible iframe border)
    - Logs both the message type and the full payload
    - Has a "Clear Log" button
    - Shows instructions: "1. Click 'Send Coupon Config', 2. Play the game, 3. Win all 5 rounds, 4. Check the log for RPS_GAME_WIN"

    The page should be simple, functional HTML with inline CSS and JS. No external dependencies.
    Use a visible border (#ccc) on the iframe so it's clear what is the game vs the test page.
    The iframe should wait for its `load` event before the "Send Coupon Config" button becomes active.
  </action>
  <verify>
    <automated>cd /Users/nonsleeper/Documents/Coding/260330 && grep -q "frame-ancestors" next.config.ts && grep -q "X-Frame-Options" next.config.ts && grep -q "ALLOWALL" next.config.ts && test -f public/test-embed.html && grep -q "RPS_COUPON_CONFIG" public/test-embed.html && grep -q "RPS_GAME_WIN" public/test-embed.html && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "frame-ancestors" next.config.ts
    - grep -q "X-Frame-Options" next.config.ts
    - grep -q "headers()" next.config.ts
    - grep -q "NEXT_PUBLIC_ALLOWED_ORIGIN" next.config.ts
    - test -f public/test-embed.html
    - grep -q "RPS_COUPON_CONFIG" public/test-embed.html
    - grep -q "RPS_GAME_WIN" public/test-embed.html
    - grep -q "iframe" public/test-embed.html
    - grep -q "postMessage" public/test-embed.html
  </acceptance_criteria>
  <done>
    next.config.ts allows iframe embedding from all origins via CSP frame-ancestors * and X-Frame-Options ALLOWALL.
    NEXT_PUBLIC_ALLOWED_ORIGIN env var can restrict origins in production.
    test-embed.html embeds the game in a 375x667 iframe, sends coupon config, and logs win events.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of full embed integration flow</name>
  <files>none</files>
  <action>
    Human visually verifies the full embed integration flow. No code changes in this task -- verification only.
  </action>
  <what-built>
    Complete embed integration: coupon victory screen, postMessage bidirectional communication, iframe embedding with headers, and test harness page.
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Open `http://localhost:3000/test-embed.html` in browser
    3. Click "Send Coupon Config" button
    4. Inside the iframe, play the game (click "도전 시작", then win 5 rounds)
       - Tip: You may need to play multiple times since AI win rates make 5 consecutive wins difficult
    5. **Verify on victory:**
       - Coupon code "TEST-COUPON-2026" is visible on the victory screen
       - Coupon text "10% 할인 쿠폰" is visible
       - Confetti animation still plays
    6. **Verify in the log panel:**
       - A `RPS_GAME_WIN` entry appears with sessionId, rounds array, completedAt, totalPlayTimeMs
    7. Click "한 번 더 하기" (retry), win again
       - Coupon should still appear (persisted across retry)
    8. **Verify standalone:** Open `http://localhost:3000` directly
       - Win 5 rounds -- should show "5판 연속 승리!" without coupon (no parent sent config)
    9. **Verify no console errors** in both modes
  </how-to-verify>
  <verify>Human confirms all 9 verification steps pass</verify>
  <done>Full embed integration verified end-to-end by human tester</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All tests pass: `npx vitest run`
- Types compile: `npx tsc --noEmit`
- ResultScreen shows coupon when config present, fallback when absent
- next.config.ts returns iframe-permissive headers
- test-embed.html demonstrates full bidirectional postMessage flow
- Game works standalone (no coupon config) and embedded (with coupon config)
</verification>

<success_criteria>
- INTG-01: Victory screen displays coupon code/image when couponConfig received from parent
- INTG-03: Game loads in iframe without X-Frame-Options blocking, CSS containment prevents leakage
- Coupon display is graceful: shows coupon when available, "5판 연속 승리!" when not
- test-embed.html serves as QA tool for the full integration contract
- Human verification confirms end-to-end flow works
</success_criteria>

<output>
After completion, create `.planning/phases/03-embed-integration/03-02-SUMMARY.md`
</output>
